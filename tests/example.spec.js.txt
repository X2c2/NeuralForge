const { test, expect } = require('@playwright/test');

test('homepage loads', async ({ page }) => {
  await page.goto('https://neural-forge-eight.vercel.app/');
  await expect(page).toHaveTitle(/NeuralForge/);
});